public class arrays {
    public static void main(String args[]){
// aray is a collection of simmilar datatypes which is stored in contiguos memory location ,it has a fixed size and it is in the package named java.util.array.the indexing starts from 0
        int arr[]={22,33,44,22};

        for(int i=0;i<arr.length;i++){
            System.out.println(arr[i]);

        }


    }
}
